import { GridElemDirective } from './grid-elem.directive';

describe('GridElemDirective', () => {
  it('should create an instance', () => {
    const directive = new GridElemDirective();
    expect(directive).toBeTruthy();
  });
});
